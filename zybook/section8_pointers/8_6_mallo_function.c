// malloc function allocate a memory inside the heap
// malloc (# of bytes)

// dynamic memory: dynamically define/allocate memory according to the user's input

// size() is an operator
// allocate required number of bytes: malloc(sizeof(int))

